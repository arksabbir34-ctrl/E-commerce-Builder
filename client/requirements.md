## Packages
zustand | Global state management for the shopping cart (persisted to localStorage)

## Notes
Assuming `@shared/routes` exports the `api` object and `buildUrl` helper as defined in the routes manifest.
Assuming `@shared/schema` exports DB types like `Product`, `InsertProduct`, `InsertContact`, `InsertOrder`.
